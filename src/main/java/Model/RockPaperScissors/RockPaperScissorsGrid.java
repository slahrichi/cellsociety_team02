package Model.RockPaperScissors;

import Model.Grid;
/**
 * class that extends the `Grid` superclass for modeling Rock-Paper-Scissors
 *
 * @author Matthew Giglio
 */
public class RockPaperScissorsGrid extends Grid {

  /**
   * @param numberOfRows number of rows in grid
   * @param numberOfColumns number of columns in grid
   */
  public RockPaperScissorsGrid(int numberOfRows, int numberOfColumns) {
    super(numberOfRows, numberOfColumns);
  }
}
